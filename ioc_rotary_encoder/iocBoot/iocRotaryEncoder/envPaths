# envPaths - EPICS IOC environment paths
# This file is automatically generated during IOC build

# IOC base directory
epicsEnvSet("IOC", "$(TOP)/../..")
epicsEnvSet("TOP", "$(IOC)/RotaryEncoderApp")

# EPICS base directory
epicsEnvSet("EPICS_BASE", "/opt/epics/base")

# Common directory with dbd, template files
epicsEnvSet("COMMON_DIR", "$(TOP)/..")

# Application directory
epicsEnvSet("APPDIR", "$(IOC)/iocBoot/iocRotaryEncoder")
